@model MvcApplication.Models.TaskGroup

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>TaskGroup</legend>

        @Html.HiddenFor(model => model.TaskGroupID)

        <div class="editor-label">
            @Html.LabelFor(model => model.TaskGroupName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.TaskGroupName)
            @Html.ValidationMessageFor(model => model.TaskGroupName)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
